spring:
  profiles: demo
  datasource:
    url: jdbc:h2:~/demo;MODE=MSSQLServer;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
    driverClassName: org.h2.Driver
  h2:
    console:
      enabled: true
      path: /h2

server:
  port: 8090

log:
  path: c:\\rt\\logs\\demo

settings:
  multiLanguage: true
